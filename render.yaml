services:
  - type: worker
    name: sobral-invest
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run streamlit.py --server.enableCORS false --server.enableXsrfProtection false
